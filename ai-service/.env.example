# MySQL read-only connection (ai_reader account)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=hotel_db
DB_USER=ai_reader
DB_PASSWORD=ai_reader_pass

# Embedding model (local, no internet needed after first download)
SPLADE_MODEL=naver/splade-cocondenser-ensembledistil

# Service port
AI_SERVICE_PORT=8000
